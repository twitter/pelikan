[package]
name = "protocol-redis-fuzz"
version = "0.0.0"
authors = ["Automatically generated"]
publish = false
edition = "2018"

[package.metadata]
cargo-fuzz = true

[dependencies]
libfuzzer-sys = "0.4"
config = { path = "../../../config" }
protocol-common = { path = "../../common" }

[dependencies.protocol-redis]
path = ".."

# Prevent this from interfering with workspaces
[workspace]
members = ["."]

[[bin]]
name = "redis"
path = "fuzz_targets/redis.rs"
test = false
doc = false
